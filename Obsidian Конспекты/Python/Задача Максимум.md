```python
# Считываем два целых числа 
n и m из ввода 2n, m = [int(i) for i in input().split()] 3 4
# Считываем матрицу a размером 
n x m 5a = [[int(j) for j in input().split()] for i in range(n)] 6 7
#Инициализируем переменные для хранения индексов максимального элемента 8best_i, best_j = 0, 0 9 10
#Инициализируем текущий максимальный элемент первым элементом матрицы 11curr_max = a[0][0] 12 13
# Проходим по всем элементам матрицы 
14for i in range(n): 15 for j in range(m): 16 # Если текущий элемент больше текущего максимума 17 
if a[i][j] > curr_max: 18 
# Обновляем текущий максимум 
19 curr_max = a[i][j] 20 
# Сохраняем индексы максимального элемента 
21 best_i, best_j = i, j 22 23
# Выводим индексы максимального элемента
24print(best_i, best_j)
``` 

